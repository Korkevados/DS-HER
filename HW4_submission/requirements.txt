# HW4 submission — local Mac (CPU) environment.
# Python 3.9+. Install into a venv:  python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
numpy>=1.24,<2.0
pandas>=2.0,<2.3
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
torch>=2.0          # CPU build is fine; no GPU needed
jupyter>=1.0
nbconvert>=7.0
nbformat>=5.7
ipykernel>=6.0
python-docx>=1.1
flask>=3.0          # live demo app
flask-cors>=4.0
python-pptx>=0.6.23 # deck readback / content QA (markitdown needs py3.10+, unavailable here)
