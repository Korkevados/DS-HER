# torch is installed separately in the Dockerfile from the CPU-only index
# (keeps the image small — no CUDA). Versions pinned for reproducible inference.
flask==3.1.3
flask-cors==6.0.5
gunicorn==23.0.0
numpy==2.0.2
scipy==1.13.1
